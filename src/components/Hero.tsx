export function Hero() {
  return (
    <section className="hero">
      <div className="eyebrow">Constraint-based composition</div>
      <h1>
        One creative.
        <span className="accent-line">Every surface.</span>
      </h1>
      <p>
        One declarative ad specification. Multiple constrained surfaces. One deterministic resolver that decides
        what survives, what shrinks, and what gets dropped — and can explain why.
      </p>
    </section>
  );
}
